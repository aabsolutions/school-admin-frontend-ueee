import { Route } from '@angular/router';
import { MessagingComponent } from '@shared/components/messaging/messaging.component';

export const STUDENT_MESSAGING_ROUTE: Route[] = [
  { path: '', component: MessagingComponent },
];
